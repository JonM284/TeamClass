using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DummyObj : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Portrait.canSelect = true; 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
